# Python Algorithms & Data Structures

Collection of algorithms and data structures implemented in Python for learning and practice.

## 📚 Contents

### `imperativa.py` - Imperative Solutions

**Array & List Operations:**
- `maior2()` - Find second largest element in array
- `duplicados()` - Find duplicate elements
- `intersecao()` - Intersection of two lists
- `remove_duplicados()` - Remove duplicates preserving order
- `roda()` - Rotate list by n positions
- `inverte()` - Reverse list
- `pares_inverso()` - Extract even numbers in reverse order

**Dictionary Operations:**
- `conta_pal()` - Word frequency counter
- `filtra()` - Filter dictionary by value threshold
- `inverte()` - Invert dictionary (swap keys/values)
- `merge()` - Merge two dictionaries with value addition
- `agrupa_anagramas()` - Group anagrams together
- `agrupa_por_tamanho()` - Group words by length

**String Processing:**
- `is_palindrome()` - Check if string is palindrome
- `largest_substring()` - Find largest common substring between two strings

**Dynamic Programming:**
- `fib_iterativo()` - Fibonacci sequence (iterative approach)
- `lis()` - Longest Increasing Subsequence
- `min_coins()` - Coin change problem (minimum coins needed)

### `recursiva.py` - Recursive Solutions

- `fatorial()` - Factorial calculation
- `reverse()` - Reverse string recursively
- `fibo()` - Fibonacci sequence (recursive approach)
- `soma()` - Sum of list elements
- `conta()` - Count occurrences of element in list
- `flatten()` - Flatten nested lists
- `power()` - Power calculation (x^n)

## 🎯 Learning Focus

- **Problem-solving patterns** - Common algorithmic approaches
- **Time/space complexity** - Understanding trade-offs
- **Iterative vs Recursive** - Different implementation strategies
- **Dynamic programming** - Optimization techniques
- **Data structure operations** - Practical applications

## 🚀 Usage
```python
# Run the files directly
python imperativa.py
python recursiva.py

# Or import specific functions
from imperativa import lis, min_coins
from recursiva import fatorial, flatten
```

## 📖 Background

These implementations were developed as part of my computer science fundamentals learning path in Biomedical Engineering at Instituto Superior Técnico.

---

**Francisco Marques**  
*Biomedical Engineering @ IST, Lisbon*
